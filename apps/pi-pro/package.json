{
  "name": "pi-pro",
  "version": "0.1.0",
  "description": "Sid's improved coding agent — TS fork of pi-mono. v0.5.0 Token/Cost Foundation + v0.6.0 Agent Swarm v1.",
  "type": "module",
  "bin": {
    "pi": "./bin/pi"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pi/bench": "workspace:*",
    "@pi/optimizer": "workspace:*",
    "@pi/provider": "workspace:*",
    "@pi/subagent": "workspace:*",
    "@pi/swarm": "workspace:*",
    "@pi/tasks": "workspace:*",
    "@pi/tools": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
