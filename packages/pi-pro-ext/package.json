{
  "name": "@pi-pro/ext",
  "version": "0.2.3",
  "type": "module",
  "description": "pi-pro v0.2.0 — Sid's improved coding agent as a pi-mono extension. Adds agent modes, plan mode, todo tool, config, memory, starship footer, git status icons, runtime detection, and plan widget on top of @earendil-works/pi-coding-agent.",
  "keywords": [
    "pi-package"
  ],
  "main": "./index.ts",
  "files": [
    "index.ts",
    "package.json"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@pi-pro/config": "workspace:*",
    "typebox": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
