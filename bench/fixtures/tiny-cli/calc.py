"""
This file is intentionally a copy of src_calc.py. The point of the
add-tests-legacy task is to write pytest cases for the calc module.
"""
import sys
sys.path.insert(0, ".")

from src_calc import add, subtract, multiply, divide, power
