# tiny-cli

Synthetic Python CLI fixture. `src_calc.py` has untested arithmetic
functions. Task: `add-tests-legacy` — write pytest cases (the fixture
ships a starter `test_calc.py` that the agent will improve or extend).

Run tests: `npm test` (delegates to pytest)
