---
type: daily
date: 2026-06-12
---

# 2026-06-12

## Hard wipe + clean reinstall (v0.1.0)

Sid: "ok lets do this wipe the entire thing ok, start with a new pi from https://pi.dev/, and do the installations and changes properly"

### What happened

- Wiped old 19-package monorepo at `/home/trader/Developer/pi-pro`
- Wiped all install state: `~/.local/bin/pi` symlink, `~/.pi/` (settings, auth, config, sessions), `~/.npm-global/@earendil-works/pi-coding-agent`
- Cleaned `~/.bashrc` (removed our PATH prepend and the promyra alias)
- Installed official `pi` v0.79.1 via `curl https://pi.dev/install.sh | sh` (no manual npm)
- Re-init monorepo: minimal `packages/pi-pro-ext/` + `packages/config/`
- Wrote single-file extension (index.ts) that uses the pi-mono ExtensionAPI
- `pi install ./packages/pi-pro-ext` registers the extension in `~/.pi/agent/settings.json`

### Architecture pivot

Previous architecture (pre-wipe): 19 packages, 1321 tests, standalone `pi` binary, complex v0.8.4 release.

New architecture (v0.1.0): single extension, 2 internal packages (extension + config), minimal. Sid's directive: "extension, not a new one."

### State of the world

- `pi --version` → 0.79.1
- `pi` REPL loads with pi-pro extension
- v0.1.0 features: `:mode`, `:plan`, `:todos`, `:config`, `:doctor`, `todo` tool
- No manual symlinks, no PATH hacks
- Live LLM bench still blocked (Go subscription not active at opencode.ai/auth)
